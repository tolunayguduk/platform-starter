/**
 * Empty on purpose - reserves the module-per-schema boundary (own schema: platform_post) before
 * this module has any code. Follow README.md's "Yeni bir modül eklemek" and mirror module-user's
 * layer-by-package layout: entity/repository/constant/controller(+model)/service(+model)/mapper.
 */
package com.platform.post;
